'use client';

// DLBC dashboard settings — reuses the same ProfileSettings component
// pointing to the shared profile table (branch_id scoping is on data tables, not profile itself)
export { default } from '@/app/dashboard/settings/page';
